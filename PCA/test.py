import numpy
import numpy as np
# 定义一个列表
numbers = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]

# 使用sorted()函数按降序排序，并获取原始下标
# sorted_numbers_with_indices = sorted(enumerate(numbers), key=lambda b: b[1], reverse=True)
#
# # 打印排序后的列表及其原始下标
# for index, value in sorted_numbers_with_indices:
#     print(f"下标 {index}: {value}")
zp = np.zeros((2,0))
print(zp)